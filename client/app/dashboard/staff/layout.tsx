'use client';

export default function StaffDirectoryLayout({ children }: { children: React.ReactNode }) {
    return (
        <div className="h-full">
            {children}
        </div>
    );
}
